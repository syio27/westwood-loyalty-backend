spring:
  config:
    activate:
      on-profile: dev
  
  datasource:
    url: jdbc:h2:mem:devdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  
  h2:
    console:
      enabled: true
      path: /h2-console
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true
  
  # Email Configuration for Development (can use MailHog, Mailtrap, or Gmail)
  mail:
    host: ${SMTP_HOST:smtp.gmail.com}
    port: ${SMTP_PORT:587}
    username: ${SMTP_USERNAME:noreplywestwood@gmail.com}
    password: ${SMTP_PASSWORD:oebwrasytigojqbm}
    properties:
      mail:
        smtp:
          auth: ${SMTP_AUTH:true}
          starttls:
            enable: ${SMTP_STARTTLS:true}
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

jwt:
  secret: ${JWT_SECRET:2GsAZVNOcd0lYajbxzwiP5dxk9sDAcr/GGCEzsqvlA0=}
  expiration: ${JWT_EXPIRATION:86400000}

# Application Configuration
app:
  frontend:
    url: ${FRONTEND_URL:http://localhost:4200}
  mail:
    from-name: ${MAIL_FROM_NAME:Westwood Team}
  registration:
    enabled: true  # Allow registration in dev

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

