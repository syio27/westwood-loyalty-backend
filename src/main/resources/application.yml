spring:
  application:
    name: tinta-backend
  
  profiles:
    active: dev
  
  # Email Configuration (SMTP)
  mail:
    host: smtp.gmail.com
    port: 587
    username: noreplywestwood@gmail.com
    password: oebwrasytigojqbm
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET:2GsAZVNOcd0lYajbxzwiP5dxk9sDAcr/GGCEzsqvlA0=}
  access-token-expiration: ${JWT_ACCESS_TOKEN_EXPIRATION:900000}  # 15 minutes
  refresh-token-expiration: ${JWT_REFRESH_TOKEN_EXPIRATION:604800000}  # 7 days

# Application Configuration
app:
  # Where to serve Angular static files from. In production use classpath (default).
  # In dev (application-dev.yml) overridden to filesystem so ng build updates are picked up without restart.
  static:
    browser-path: classpath:/static/browser/
  frontend:
    url: ${FRONTEND_URL:http://localhost:4200}
  mail:
    from-name: ${MAIL_FROM_NAME:Westwood Team}
  cookie:
    domain: ${COOKIE_DOMAIN:}
    secure: ${COOKIE_SECURE:false}  # false for localhost, true for production
    same-site: ${COOKIE_SAME_SITE:Lax}  # Lax for localhost cross-origin, None for production with Secure=true
  refresh-token:
    cleanup-cron: ${REFRESH_TOKEN_CLEANUP_CRON:0 0 2 * * ?}  # Daily at 2 AM

logging:
  level:
    root: INFO
    org.springframework.web: INFO
    org.hibernate: INFO
    org.springframework.security: DEBUG
    com.westwood.security: DEBUG

